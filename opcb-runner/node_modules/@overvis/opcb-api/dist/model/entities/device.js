"use strict";
/* eslint-disable @typescript-eslint/no-explicit-any */
/* eslint-disable @typescript-eslint/no-unsafe-return */
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueryModel = void 0;
const base_1 = require("./base");
class QueryModel extends base_1.QueryModelBase {
    getById(id) {
        return this.getQuery(`select * from devices where id = @id;`).get({ id });
    }
    insert(args) {
        const res = this.getQuery(`insert into devices (title, orig_template_id, channel)
            values (@title, @origTemplateId, @channel) returning id;`).get(args);
        return res.id;
    }
    delete(id) {
        this.getQuery(`delete from devices where id = @id;`).run({ id });
    }
    updateTitle(args) {
        this.getQuery(`update devices set title = @title where id = @id;`).run(args);
    }
}
exports.QueryModel = QueryModel;
//# sourceMappingURL=device.js.map