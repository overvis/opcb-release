"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.up = exports.name = void 0;
const tslib_1 = require("tslib");
const path = tslib_1.__importStar(require("path"));
const __1 = require("..");
exports.name = path.basename(__filename);
async function up(_config) {
    const modbusRs485 = _config.modbusRs485;
    for (let n = 0; n < modbusRs485.length; n++) {
        const props = modbusRs485[n];
        if (!props.deviceName) {
            __1.logger.warn(`modbusRs485[${n}]: Parameter 'deviceName' is empty`);
            continue;
        }
        const value = `/dev/${props.deviceName}`;
        if (props.devicePath) {
            __1.logger.warn(`modbusRs485[${n}]: Parameter 'devicePath' is already exist (equal to "${props.devicePath}"), not changing.`);
            continue;
        }
        else {
            __1.logger.warn(`modbusRs485[${n}]: Adding new parameter 'devicePath=${value}'`);
        }
        props.devicePath = value;
    }
    return Promise.resolve();
}
exports.up = up;
//# sourceMappingURL=001-add-device-path.js.map