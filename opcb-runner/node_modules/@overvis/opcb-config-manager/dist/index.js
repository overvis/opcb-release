"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = exports.logger = void 0;
const opcb_ts_shared_1 = require("@overvis/opcb-ts-shared");
const server_tools_1 = require("@overvis/server-tools");
const encrypt_1 = require("./encrypt");
const file_migrations_1 = require("./file-migrations");
const dev_state_1 = require("./handlers/dev-state");
const reset_config_1 = require("./handlers/reset-config");
const set_config_1 = require("./handlers/set-config");
const logic_1 = require("./logic");
const manufacturer_1 = require("./manufacturer");
const readconf_1 = require("./readconf");
const static_1 = require("./static");
async function run(setLogger, runConf) {
    exports.logger = setLogger;
    exports.logger.info("Starting...");
    const bus = runConf.redisClient;
    // read config
    const [confRecord, hasChangesFromFactory] = await (0, readconf_1.readConfigFile)(runConf.configFile, runConf.factoryConfigFile);
    const migrationsApplied = await (0, file_migrations_1.applyFileMigrations)(confRecord);
    const [opcbConfig, validate] = await (0, readconf_1.validateAndFixConfig)(confRecord, runConf.configFile, hasChangesFromFactory || migrationsApplied);
    (0, server_tools_1.setSentryContext)({
        pinCode: opcbConfig.overvisVpn.pinCode || "no pin",
        mac: opcbConfig.elan.mac || "unknown mac",
    });
    await (0, encrypt_1.encryptPasswordsInConfigFile)(opcbConfig, runConf.configFile);
    // populate config
    const flatconf = (0, logic_1.flattenConfig)(opcbConfig);
    await bus.setConfs({ "*": null });
    await bus.setConfs(flatconf);
    // read manufacturer file
    const manufacturer = await (0, manufacturer_1.readManufacturerLicenseFile)(runConf.manufacturerFile);
    // set state
    const stateChange = (0, static_1.getStaticInfo)(manufacturer);
    // calculate and set routing
    const routes = (0, logic_1.calculateRouting)(opcbConfig);
    await bus.setState(Object.assign(stateChange, routes, {
        boundToOcpServer: opcbConfig.overvisVpn.boundToOcpServer,
        lastRunVersion: opcbConfig.global.lastRunVersion,
        "overvisRc:mac": opcbConfig.elan.mac || undefined,
        "release:model": opcbConfig.global.model,
        "release:modelName": opcbConfig.global.modelName,
        "virtualDevice:uid": opcbConfig.virtualDevice.uid,
        "virtualDevice:isEnabled": opcbConfig.virtualDevice.isEnabled,
    }));
    // subscribe to config-related events in bus
    const state = { config: opcbConfig };
    bus.registerRequestHandler("set-config", (0, set_config_1.createSetConfigHandler)(bus, state, validate, runConf.configFile, runConf.factoryConfigFile));
    bus.registerRequestHandler("reset-config", (0, reset_config_1.createResetConfigHandler)(bus, state, runConf.factoryConfigFile, runConf.configFile, runConf.manufacturerFile));
    bus.registerRequestHandler("dev-state", (0, dev_state_1.createDevStateHandler)(bus, state, validate, runConf.configFile));
    // TODO_FUTURE: write last known version to config, it will run sync
    // run sync
    await bus.sendMsg(new opcb_ts_shared_1.redisCmd.SyncConfig());
    await bus.sendMsg(new opcb_ts_shared_1.redisCmd.SyncRouting());
    await bus.sendMsg(new opcb_ts_shared_1.redisCmd.SyncTasks());
    exports.logger.info("Started.");
}
exports.run = run;
//# sourceMappingURL=index.js.map