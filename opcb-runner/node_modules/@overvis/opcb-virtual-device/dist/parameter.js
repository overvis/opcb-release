"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createModbusLoraSensParameters = exports.createModbusUserParameters = exports.createModbusParameters = exports.createAddressParamMap = void 0;
const USER_FREE_PARAMS_QUANTITY = 200;
function createAddressParamMap(schema, map, source, prevKey, prevAddressSpacing) {
    var _a, _b;
    const properties = (schema.items ? schema.items.properties : schema.properties);
    if (schema.items) {
        if (!schema.modbusArrayAddressSpacing) {
            return map;
        }
        const maxItems = schema.maxItems;
        const addressSpacing = schema.modbusArrayAddressSpacing;
        for (const [key, value] of Object.entries(properties)) {
            const subSchema = value;
            for (let i = 0; i < maxItems; i++) {
                const path = prevKey ? `${prevKey}:${i}:${key}` : `${key}${i}`;
                if (subSchema.properties || subSchema.items) {
                    createAddressParamMap(subSchema, map, source, path, addressSpacing * i);
                }
                else if (subSchema.mapper) {
                    const addresses = subSchema.mapper.getRegisterAddresses();
                    const mapper = subSchema.mapper.shiftAddress(addressSpacing * i);
                    for (const address of addresses) {
                        const addressNum = address + i * addressSpacing;
                        map.set(addressNum, {
                            path,
                            mapper,
                            source: ((_a = subSchema.source) !== null && _a !== void 0 ? _a : source) || null,
                        });
                    }
                }
            }
        }
    }
    else if (schema.properties) {
        for (const [key, value] of Object.entries(properties)) {
            const subSchema = value;
            const path = subSchema.path ? subSchema.path : prevKey ? `${prevKey}:${key}` : key;
            if (subSchema.properties || subSchema.items) {
                createAddressParamMap(subSchema, map, source, path);
            }
            else if (subSchema.mapper) {
                let addresses = subSchema.mapper.getRegisterAddresses();
                let mapper = subSchema.mapper;
                if (prevAddressSpacing) {
                    mapper = subSchema.mapper.shiftAddress(prevAddressSpacing);
                    addresses = addresses.map((address) => address + prevAddressSpacing);
                }
                for (const address of addresses) {
                    map.set(address, {
                        path,
                        mapper,
                        source: ((_b = subSchema.source) !== null && _b !== void 0 ? _b : source) || null,
                    });
                }
            }
        }
    }
    return map;
}
exports.createAddressParamMap = createAddressParamMap;
function createModbusParameters(schema, source, state, prevKey, prevAddressSpacing, arrayGroupName) {
    var _a, _b;
    const properties = (schema.items ? schema.items.properties : schema.properties);
    if (schema.items) {
        if (!schema.modbusArrayAddressSpacing) {
            return state;
        }
        const maxItems = schema.maxItems;
        const addressSpacing = schema.modbusArrayAddressSpacing;
        for (const [key, value] of Object.entries(properties)) {
            const subSchema = value;
            for (let i = 0; i < maxItems; i++) {
                const path = prevKey ? `${prevKey}:${i}:${key}` : `${key}${i}`;
                arrayGroupName = schema.name ? `${schema.name} ${i + 1}` : "";
                if (subSchema.properties || subSchema.items) {
                    createModbusParameters(subSchema, source, state, path, i * addressSpacing, arrayGroupName);
                }
                else if (subSchema.mapper) {
                    const registers = subSchema.mapper
                        .getRegisterAddresses()
                        .map((a) => a + i * addressSpacing);
                    const paramSource = (_a = subSchema.source) !== null && _a !== void 0 ? _a : source;
                    const type = paramSource === "state" ? "input" : "holding";
                    const name = `${arrayGroupName} - ${subSchema.name}`;
                    state.push({
                        name,
                        registers,
                        description: subSchema.description,
                        type: subSchema.regType || type,
                        group: subSchema.group,
                        redisPath: path,
                        source: paramSource,
                        units: subSchema.units,
                        multiplier: subSchema.multiplier,
                        decimalPlaces: subSchema.decimalPlaces,
                    });
                }
            }
        }
    }
    else if (schema.properties) {
        for (const [key, value] of Object.entries(properties)) {
            const subSchema = value;
            const path = subSchema.path ? subSchema.path : prevKey ? `${prevKey}:${key}` : key;
            if (subSchema.properties || subSchema.items) {
                createModbusParameters(subSchema, source, state, path);
            }
            else if (subSchema.mapper) {
                const paramSource = (_b = subSchema.source) !== null && _b !== void 0 ? _b : source;
                const type = paramSource === "state" ? "input" : "holding";
                let mapper = subSchema.mapper;
                if (prevAddressSpacing) {
                    mapper = subSchema.mapper.shiftAddress(prevAddressSpacing);
                }
                const name = subSchema.name;
                state.push({
                    name: arrayGroupName ? `${arrayGroupName} - ${name}` : name,
                    registers: mapper.getRegisterAddresses(),
                    description: subSchema.description,
                    type: subSchema.regType || type,
                    group: subSchema.group,
                    redisPath: path,
                    source: paramSource,
                    units: subSchema.units,
                    multiplier: subSchema.multiplier,
                    decimalPlaces: subSchema.decimalPlaces,
                });
            }
        }
    }
    return state;
}
exports.createModbusParameters = createModbusParameters;
function createModbusUserParameters() {
    const result = [];
    for (let i = 0; i < USER_FREE_PARAMS_QUANTITY; i++) {
        result.push({
            name: `User register ${i}`,
            registers: [10000 + i],
            description: `User register ${i}`,
            type: "holding",
            group: "User saved values",
            hiddenDescription: true,
            source: "modbus",
            redisPath: `${10000 + i}`,
        });
    }
    return result;
}
exports.createModbusUserParameters = createModbusUserParameters;
function createModbusLoraSensParameters(weightModules) {
    const result = [];
    // one weight module containe 8 weight units and each weight unit has 9 registers
    for (const [i, moduleId] of weightModules.split(",").entries()) {
        for (let j = 0; j < 8; j++) {
            // weight
            result.push({
                name: `Weight unit ${j + 1}`,
                moduleId,
                paramId: `weight_${j + 1}_g`,
                register: 11000 + i * 72 + j * 9,
                index: 0,
                type: "u32",
            });
            result.push({
                name: `Weight unit ${j + 1}`,
                moduleId,
                paramId: `weight_${j + 1}_g`,
                register: 11001 + i * 72 + j * 9,
                index: 1,
                type: "u32",
            });
            // counts
            result.push({
                name: `Counts ${j + 1}`,
                moduleId,
                paramId: `items_${j + 1}`,
                register: 11002 + i * 72 + j * 9,
                index: 0,
                type: "u16",
            });
            // one sku weight
            result.push({
                name: `SKU weight ${j + 1}`,
                moduleId,
                paramId: `weight_${j + 1}_g`,
                register: 11003 + i * 72 + j * 9,
                index: 0,
                type: "u32",
                isSettings: true,
            });
            result.push({
                name: `SKU weight ${j + 1}`,
                moduleId,
                paramId: `weight_${j + 1}_g`,
                register: 11004 + i * 72 + j * 9,
                index: 1,
                type: "u32",
                isSettings: true,
            });
            // loaded
            result.push({
                name: `Loaded ${j + 1}`,
                moduleId,
                paramId: `loaded_${j + 1}`,
                register: 11005 + i * 72 + j * 9,
                index: 0,
                type: "u32",
            });
            result.push({
                name: `Loaded ${j + 1}`,
                moduleId,
                paramId: `loaded_${j + 1}`,
                register: 11006 + i * 72 + j * 9,
                index: 1,
                type: "u32",
            });
            // unloaded
            result.push({
                name: `Unloaded ${j + 1}`,
                moduleId,
                paramId: `unloaded_${j + 1}`,
                register: 11007 + i * 72 + j * 9,
                index: 0,
                type: "u32",
            });
            result.push({
                name: `Unloaded ${j + 1}`,
                moduleId,
                paramId: `unloaded_${j + 1}`,
                register: 11008 + i * 72 + j * 9,
                index: 1,
                type: "u32",
            });
        }
    }
    return result;
}
exports.createModbusLoraSensParameters = createModbusLoraSensParameters;
//# sourceMappingURL=parameter.js.map